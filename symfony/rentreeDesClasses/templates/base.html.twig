<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}LoveFinder{% endblock %}</title>
    {% block stylesheets %}
    <link href="{{asset('css') ~ '/main.css'}}" rel="stylesheet">
    {% endblock %}
</head>

<body>
    <nav>
    <ul>
        <li><a href="{{path('home')}}">Accueil</a></li> 
        {% if app.user %}
        <li><a href="{{path('user_panel')}}">Espace personnel</a></li>
        <li><a href="{{path('app_logout')}}">Deconnexion</a></li>
        {% else %}
        <li><a href="{{path('app_login')}}">Connexion</a></li>
        {% endif %}
        
    </ul>
    </nav>
    {% block body %}{% endblock %}
    {% block javascripts %}{% endblock %}
</body>

</html>